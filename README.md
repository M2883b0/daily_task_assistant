# 每日任务助手（Daily Task Assistant）

一个基于 `tkinter` 的 Windows 桌面小工具，用于记录每日任务、快速勾选进度，并按周导出可直接给 AI 使用的周报提示词。

## 功能简介

- 每日任务新增、勾选完成、删除、双击行内编辑
- 草稿页支持 Markdown 实时渲染（当前光标行保持源码编辑）
- 草稿页支持 Markdown/源码双视图切换
- 草稿页支持工具下拉菜单（JSON 格式化、Base64 编解码、URL 编解码）
- 草稿页支持字数统计与选中字符统计
- 草稿页支持查找替换（Ctrl+F / Ctrl+H）与匹配高亮
- 日志页支持单日查看与周汇总
- 周汇总可一键复制 AI 周报提示词
- 支持自定义周报提示词模板（含 `{tasks_block}` 占位符）
- 支持全局热键唤起窗口、仅应用聚焦时生效的页内快捷键、窗口透明度设置

## 最近更新（自上次提交以来）

- 草稿编辑器升级为“所见即所得 + 当前行源码编辑”模式，提升 Markdown 编写体验
- 新增草稿区视图切换：可在工具栏手动切换 `Markdown` 与 `源码` 模式
- 新增草稿统计信息：显示源码字数与当前选中字符数
- 优化草稿搜索替换流程：搜索时自动进入源码视图，关闭后恢复原视图模式
- 完善草稿编辑交互：改进回车、撤销/重做、Esc 行为与内容保存一致性
- 草稿工具栏改为点击下拉菜单，工具仅作用于已选中区域；新增 Base64 编解码与 URL 编解码
- 新增草稿工具“局部快捷键”（仅草稿区/应用聚焦时生效），可在设置中配置：JSON / Base64 / URL
- 草稿查找替换支持正则（高亮/查找/替换/全部替换），并修复 Markdown 模式下粘贴丢失
- 代码块/公式块渲染增强：仅闭合后才渲染；代码块显示源码、行号、边框与基础语法高亮
- 代码块/公式块输入体验增强：代码围栏/`$$` 回车自动补全闭合；公式/行内公式提供 LaTeX 文本化解析与高亮
- 草稿代码块内通用补全：缩进回车、`{}`、`()`、`'`/`"` 成对补全；`Tab` 在代码块内一次等于 3 个空格
- 日志页下拉菜单与复制按钮布局优化：移除周报提示词预览框，复制按钮移动到“视图”行末尾
- 任务/设置/日志滚轮滚动与滚动条补齐，并修复渲染刷新导致的滚动位置丢失
- 设置页改为滚动容器，增加长内容可浏览；应用启动增加单实例检测（避免重复启动）

## 运行环境

- Windows 10/11
- Python 3.10+（建议）
- 仅使用标准库，无第三方依赖

## 快速启动

```bash
python daily_task_assistant.py
```

或使用无控制台入口：

```bash
pythonw daily_task_assistant.pyw
```

## 数据与配置说明

- 运行后会自动创建：
  - `data/tasks_YYYY-MM-DD.json`（每日任务）
  - `data/draft.txt`（草稿内容）
  - `data/settings.json`（当前本机配置）
  - `logs/incremental_YYYY-MM-DD.jsonl`（增量日志）
  - `logs/daily_archive.jsonl`（归档日志）
- 仓库内提供默认配置模板：`data/settings.default.json`
- Git 已忽略运行时数据与日志，避免上传个人记录

## 默认快捷键

- 显示/隐藏窗口：`CTRL + ALT + T`
- 聚焦输入框：`/`
- 切换标签页：`CTRL + TAB`
- 草稿工具快捷键（仅草稿区/应用聚焦时生效）：
  - JSON 格式化：`CTRL + ALT + J`
  - Base64 编码：`CTRL + ALT + E`
  - Base64 解码：`CTRL + ALT + D`
  - URL 编码：`CTRL + ALT + U`
  - URL 解码：`CTRL + ALT + I`

> 可在应用「设置」页内修改。

## 项目结构

```text
daily_task_assistant.py      # 主程序
daily_task_assistant.pyw     # 无控制台启动入口
draft_tools.py               # 草稿工具功能（JSON/Base64/URL）
data/settings.default.json   # 默认配置模板
.gitignore
```

